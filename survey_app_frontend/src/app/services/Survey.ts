export interface SurveyList {
        title: string,
        started_at: string,
        ended_at: string,
        created_by:number,
        is_ended: boolean,
        is_joined: boolean,
        survey_time: string
    }
